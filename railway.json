{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "pip3 install -r requirement.txt && python3 manage.py collectstatic --noinput && gunicorn framework.wsgi"
    }
}