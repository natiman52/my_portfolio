{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "pip install -r requirments.txt && python manage.py collectstatic --noinput && gunicorn framework.wsgi"
    }
}