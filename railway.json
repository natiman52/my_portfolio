{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python -m pip install -r requirement.txt && python manage.py collectstatic --noinput && gunicorn framework.wsgi"
    }
}